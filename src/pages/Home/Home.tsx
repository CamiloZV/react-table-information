import React from 'react';
import { DataTable } from '../../components';

const Home = () => {
  return (
    <>
      <h1>Tickets</h1>
      <DataTable columns={[]} rows={[]} />
    </>
  );
};

export default Home;
